/**
 * \file constants.h
 *
 * Contains constants, to be included everywhere.
 *
 * \author Created by Alex Nikanov and Federico Galbiati.
 */

/// \var TILE_SIZE Measured in motor's encoder units
const double TILE_SIZE = 100;

/// \var 90_DEGREE_ROTATION Describes the rotation needed for one side to turn the robot 90 degrees. Measured in motor's encoder units
const double _90_DEGREE_ROTATION = 100;
